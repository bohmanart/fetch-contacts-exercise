import styled from 'styled-components';

export default styled.button`
	padding: 0.5rem;
	color: var(--c-l-blue);
	background: var(--c-m-blue);
	border: none;
	border-radius: 3px;
	cursor: pointer;
`;
